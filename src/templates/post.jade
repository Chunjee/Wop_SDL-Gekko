extends ./layout.jade
block content
    section.post
      .container
        .row
          .col-lg-8.col-lg-offset-2.text-center
            h2.section-heading #{post.title}
            
            hr.light
            
            p.text-faded
              | #{post.description}
            .article
              a(href='/blog/').btn.btn-default.btn back to Blog